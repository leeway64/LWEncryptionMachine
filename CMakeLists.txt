cmake_minimum_required (VERSION 3.8)

project ("Encryption_Machine")

# Add source to this project's executable.
add_executable (EncryptionMachine "main.c" "EncryptionMachine.c" "EncryptionMachine.h")
