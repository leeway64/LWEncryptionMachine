Unit Tests
==============

To run unit tests, enter cl test_suite.c CuTest.c /link /out:test_suite.exe into the developer
command prompt for VS in the current directory to compile tests. Then, type test_suite.exe to run tests.